# .streamlit/secrets.toml
# .streamlit/secrets.toml.example  (commit this; never commit secrets.toml)
# [secrets]
# Copy this file to .streamlit/secrets.toml (locally) or paste its contents
# into the Streamlit Cloud "Secrets" panel under App Settings.
#
# DO NOT commit this file with a real key — add it to .gitignore.

COHERE_API_KEY = "your-cohere-api-key-here"

# Streamlit Cloud: Settings → Secrets → paste contents of secrets.toml